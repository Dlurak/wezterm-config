local constants = require '../constants'

local config = {}

config.color_scheme = "Tokyo Night"
config.window_background_opacity = 1
config.command_palette_bg_color = "hsl(235, 19%, 23%)"

return config
